
V2ray 安装过程

date -R  //查看系统时间

 cp  /usr/share/zoneinfo/Asia/Shanghai /etc/localtime 
 // 让服务器的时间和 China时间一致

bash <(curl -L -s https://install.direct/go.sh)

// 上面会产生 port 和 uuid

// 这里是协议配置  
 vi /etc/v2ray/config.json 
 
//默认安装不会启动的
Sudo systemctl restart v2ray

// 查看状态
Service v2ray. Status

// 客户端就可以通过vmess 协议链接上去了
